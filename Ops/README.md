# Ops

Personal operations system. Two areas:

- `System/` — the daily machinery: scheduled tasks, modules, backlog, operating system doc.
- `Career/` — goals, development plan, reviews. Gitignored — never committed.
