# agents.md — Ops (inherits /AGENTS.md)

**Scope:** `System/` (machinery) and `Career/` (goals/dev, gitignored).

## Rules
- `Career/` content is gitignored — never commit personal goals, exit intent, or performance notes.
- System changes → update the relevant module or scheduled task spec, then record a decision.
- Approved append targets: `System/Backlog.md`, `System/Operating-System.md` (Said-vs-Did tracker).
